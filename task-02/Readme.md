# task-02
here i got the image from the google and wrote a code in python to scan the text in it using pytesseract module and extracted the text and used a simple eval function to calculate the text in image
To extract the virtual environment i first activated the virtual environment after downloading it and activated by using "source env/bin/activate"

